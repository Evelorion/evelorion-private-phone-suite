# CONTIN 目录说明

私密通讯录 + 电话，两个 Android App 加一个自建后端。

## 先看哪份文档

| 你是谁 | 从哪开始 |
| --- | --- |
| **接手继续开发** | **`开发指南.md`** ← 从这里开始。踩过的坑都在那里，每条都花过一到几小时 |
| 只改通讯录 | `通讯录App/README.md` |
| 只改电话 | `电话App/README.md` |
| 改后端 / 部署 | `文档/DEPLOYMENT.md`、`文档/API.md` |
| 想搞懂密钥体系 | `文档/ARCHITECTURE.md`、`文档/DESIGN.md` |

## 目录

| 文件夹 | 是什么 |
| --- | --- |
| **通讯录App/** | 通讯录的全部源码（`com.evelorion.contacts`，Kotlin + XML 视图） |
| **电话App/** | 电话的全部源码（`com.evelorion.phone`，Kotlin + Compose） |
| **服务器后端/** | 同步服务端 + 网页端（TypeScript / Fastify） |
| **文档/** | 架构、密钥体系、部署、API 说明 |
| **安装包/** | 编译出来的 APK，按版本号排。只放成品 |
| **归档-旧版本/** | 早期基于 Fossify 分支的产物，已弃用，可以删 |

## 代码在哪、怎么编译

**源码只有一份，就是上面那三个文件夹。改代码改这里。**

**编译就用 Android Studio 打开对应文件夹，Sync 完直接 Build。**
两个都是标准 Gradle 工程。wrapper 在仓库里，签名证书只保存在本机，
不会上传 GitHub 或 VPS。
需要 JDK 17+、Android SDK 36、以及支持 AGP 9.1.0 的 Android Studio。

> Android 源码和签名材料不上传 VPS。APK 始终在本机或受控的本地构建机生成。

后端不一样：它本来就跑在 VPS 上，所以 `服务器后端/` 改完要部署过去
（构建 Docker 镜像 → 换容器）。**部署前先备份数据库。**

具体命令见 `开发指南.md` 第二节。

## 两个 App 的关系

- **同一把证书签名**（两个本地 `signing/shared.jks` 必须是同一个文件）。
  签名不同的话，两个 App 因为声明了同名的自定义权限，**装都装不上**。
- 电话 App **不单独登录**：通过 `VaultBridge` 向通讯录要 `calls` 子密钥和
  15 分钟的短期令牌。给出去的**不是主密钥**，所以电话 App 解不开任何一条联系人。
- 来电显示走**盲索引**：号码交给通讯录，它算索引查表回名字。
  索引密钥不外传 —— 交出去等于给了离线枚举任意号码的能力。

## 三件最该记住的事

1. **不要从「扫描不到」推断删除。** 出过一次事故，全部联系人在所有设备上被删光，
   最后从 SQLite 的 WAL 里逐帧回滚才捞回来。详见 `开发指南.md` 第四节。
2. **真机上拿不到 logcat**，所以两个 App 都自带崩溃记录页 ——
   崩溃后**下次启动会直接把堆栈显示出来**，带复制按钮。排查先要这个。
3. **很多坑不会报错**，只表现成「功能莫名其妙不工作」：
   图标空白、点击没反应、同步静默失效。`开发指南.md` 第三节列了 11 条。
