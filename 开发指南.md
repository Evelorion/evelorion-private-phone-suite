# 开发指南（交接文档）

写给下一个接手这个项目的开发者。

这份文档的重点**不是**「代码怎么写」——那些注释里都有。重点是三件事：

1. 项目的结构和约束
2. 怎么编译、怎么部署
3. **踩过的坑** —— 每一条都真实花掉过一到几个小时，而且大多数**不会报错**，
   只表现成「功能莫名其妙不工作」

---

## 一、项目是什么

三块东西，各自独立又必须配合：

| 目录 | 是什么 | 语言/框架 | 跑在哪 |
| --- | --- | --- | --- |
| `通讯录App/` | 联系人，端到端加密 | Kotlin + XML 视图 | Android |
| `电话App/` | 拨号、来电、通话记录 | Kotlin + Jetpack Compose | Android |
| `服务器后端/` | 零知识同步服务 + 网页端 | TypeScript / Fastify | 自建 VPS |

### 一条贯穿全局的约束

**服务器读不懂任何内容。** 它只存密文；密钥由用户的主口令派生，只存在于
手机内存里。服务器管理员、拿到数据库的人、中间人，都只能看到密文和元数据
（有多少条记录、什么时候同步的）。

这条约束解释了很多看起来绕的设计。比如「来电显示为什么要跨进程去问通讯录，
而不是电话 App 自己查号码」—— 因为算盲索引需要主密钥，而主密钥不该离开通讯录进程。

### 密钥体系（简版）

```
主口令 ──Argon2id(64MiB, t=3, p=4, NFKC)──> MK
                                             ├─HKDF "fc.kek.v1"──> KEK ──包裹──> DEK
                                             └─HKDF "fc.auth.v1"─> authSecret（登录用）

恢复码 ──> RK ─┬─HKDF "fc.rkek.v1"────────> RKEK ──也包裹同一把──> DEK
               └─HKDF "fc.auth.recovery.v1"> 恢复登录凭据

DEK ─┬─HKDF(salt=uuid, "fc.rec.v1")──────> 每条记录一把独立的键
     ├─HKDF "fc.idx.v1"──────────────────> 盲索引密钥（号码→联系人）
     └─HKDF "fc.collection.calls.v1"─────> 交给电话 App 的子密钥
```

**DEK 永不改变。** 改主口令只是用新 KEK 重新包裹一次 DEK，不需要重新加密数据。

记录加密的 AAD 绑定 `uuid(16字节) ‖ rev(4字节大端) ‖ schemaVersion(1字节)`。
**这一点在做数据恢复时会咬人**，见后面第四节。

---

## 二、怎么编译和部署

### 代码在哪

**源码只有一份，就是 `D:\CONTIN\` 下的那三个文件夹。** 改代码改这里。

### 编译 Android —— 正常路线：Android Studio

**这两个都是标准的 Gradle 工程，用 Android Studio 打开直接编译就行。**

```
Android Studio → Open → 选 D:\CONTIN\电话App （或 通讯录App）
等 Gradle Sync 完成 → Run / Build APK
```

需要的环境：

| | 版本 | 说明 |
| --- | --- | --- |
| Android Studio | 能支持 AGP 9.1.0 的版本 | 太老的版本会拒绝打开 |
| JDK | 17 或更高 | AGP 9 的最低要求 |
| Android SDK | API 36 | `compileSdk = 36` |
| Gradle | 9.4.1 | wrapper 已经带了，不用自己装 |

工程里**故意没有 `local.properties`** —— 那个文件存的是你本机的 SDK 路径，
每台机器不一样，提交进去只会让别人 sync 失败。Android Studio 首次打开时会自己生成。

签名证书 `signing/shared.jks` 只保存在本机，口令通过 `KEYSTORE_PASSWORD`、
`KEY_ALIAS` 和 `KEY_PASSWORD` 环境变量提供。**两个 App 必须用同一把证书**，
原因见坑 #4。证书、口令和 Android 源码都不能上传到 VPS。

### 部署后端

```bash
# 1. 上传到 /root/srv-build/server/
# 2. 先跑测试（131 项断言，必须全过）
docker run --rm -v /root/srv-build/server:/w -w /w node:22-alpine \
  sh -c 'node_modules/.bin/tsc --noEmit && node --experimental-strip-types test/e2e.ts'

# 3. 备份数据库（重要，见坑 #8）
/root/scripts/backup-sync.sh

# 4. 构建镜像并换容器
docker build -t contacts-sync:新标签 .
docker stop contacts-sync && docker rename contacts-sync contacts-sync-old
docker run -d --name contacts-sync --restart unless-stopped \
  --network contacts-sync_csnet -v contacts-sync_sync-data:/app/data \
  -e ... contacts-sync:新标签
```

> **换容器时注意 `sync` 这个网络别名。** compose 会自动加，手动 `docker run` 不会。
> 丢了它 nginx 解析不到上游，整个 API 会 502，而且是在你重启代理时才暴露。
> 补法：`docker network disconnect ... && docker network connect --alias sync ...`

### 线上

- API: `https://contacts.example.com`
- 反代: Caddy 或 nginx
- 数据: Docker volume 中的 `/app/data/sync.db`
- 备份: 使用定时任务保存到受保护的备份目录，真实路径不写入仓库

---

## 三、必须知道的坑

每一条都不报错，只会让你以为「代码写错了」。

### 1. 图标不显示，而且没有任何错误

官方 Material Symbols 导出的 SVG 用 `viewBox="0 -960 960 960"`，**Y 轴是负的**。
Android 的 VectorDrawable **没有 viewBox 偏移这个概念**，直接抄 pathData
会让图形整个跑到可视区外。

表现：图标位置空白，布局正常，无警告。

```xml
<vector android:viewportWidth="960" android:viewportHeight="960">
    <group android:translateY="960">   <!-- 必须 -->
        <path android:pathData="..." />
    </group>
</vector>
```

但**经典 24×24 的 Material 图标不需要这个补偿**（Y 向下为正）。
两套坐标系混用过一次，查了很久。

**判断方法：看 pathData 里的 Y 坐标是不是负数。** 是负的才需要 `translateY`。

### 2. `android:tint="?attr/..."` 在矢量图里不生效

`setImageResource()` 加载矢量图时没有主题上下文，`?attr` 解析不出来 → 透明。
同样不报错。改用 `app:tint` 或代码里 `setColorFilter()`。

### 3. Kotlin 的块注释**可以嵌套**

KDoc 里写一个 URI 模式 `/number/*`，那个 `/*` 会开一个嵌套注释，
后面整个文件被吃掉。报错是文件末尾的「Unclosed comment」，和真正位置差几百行。

同理：块注释里的 `content://xxx` 里那个 `//` **不是**行注释。
（写静态检查脚本时也要注意这一点，否则会误报。）

### 4. 自定义权限有安装顺序陷阱

请求一个当前没有任何已装应用**定义过**的权限时，请求被**静默丢弃**，
而且之后定义方装上也**不会**补授予。

所以两个 App 都必须声明同一条 `<permission>`，**逐字一致**：

```xml
<permission android:name="com.evelorion.permission.READ_PRIVATE_CONTACTS"
            android:protectionLevel="signature" />
<permission android:name="com.evelorion.permission.VAULT_BRIDGE"
            android:protectionLevel="signature" />
```

附带好处：Android 只允许签名相同的包声明同名权限。签名不一致时第二个 App 会
`INSTALL_FAILED_DUPLICATE_PERMISSION` —— 把「必须同签名」从隐性前提变成安装时的显式报错。

### 5. AGP 9 不能再加 kotlin-android 插件

AGP 9 内置 Kotlin 支持。加了 `org.jetbrains.kotlin.android` 直接报
「plugin is no longer required since AGP 9.0」编译失败。

但 **Compose 编译器插件仍然要单独声明**：`org.jetbrains.kotlin.plugin.compose`。

版本组合（`gradle/libs.versions.toml`）是验证过能编的，**不要顺手升版本**：
AGP 9.1.0 / Kotlin 2.3.10 / KSP 2.3.6 / compose-bom 2024.09.03。

### 6. `Executor.execute {}` 里抛异常 = 整个进程被杀

ThreadPoolExecutor 让异常冒到工作线程外面，走默认 UncaughtExceptionHandler。

一次真实闪退就是这么来的：同步设置页在后台线程读数据库，数据库打不开抛了
SQLiteException，用户看到的是「点开就闪退」，而崩溃点和真正原因
（数据库文件的加密状态不对）中间隔着十几层 Room 和协程的栈。

两个 App 里都有 `Bg.single(name)`，在**工作线程本身**外面包了一层。

> **新写后台任务一律用 `Bg.single()`，不要直接 `Executors.newSingleThreadExecutor()`。**

### 7. 真机上拿不到 logcat —— 所以有崩溃记录页

没插线、没 root、厂商 ROM 不给看系统日志。「闪退了」三个字查不出任何东西。

两个 App 都有 `CrashReport`：崩溃堆栈写进 `filesDir/last-crash.txt`，
**下次启动时直接显示出来**，带复制按钮。

`CrashActivity` 故意**不用主题、不碰数据库、不用 Compose** ——
它存在的意义就是别的东西都坏掉时还能显示出来，每多依赖一样东西就多一分
自己也崩掉的可能。

一次崩溃换一份完整堆栈，比任何猜测都便宜。定位问题时**先问用户要这个**。

### 8. SQLite 的 WAL 会让备份变成空壳

`sync.db` 跑在 WAL 模式下，绝大部分数据长期只存在于 `-wal` 文件里。
直接 `cp sync.db` 备出来的是一个 4 KB 空文件 —— **出事时才会发现**。

正确做法是 `VACUUM INTO`（`/root/scripts/backup-sync.sh` 就是这么做的），
它会把 WAL 内容一并合进快照。

反过来，这个特性救过一次命：删除事故之后，正是从没 checkpoint 的 WAL 里
逐帧回滚才把数据捞回来的。

### 9. R8 在小内存构建机上得不偿失

构建机 961 MB 内存，R8 每次跑 6~10 分钟，而实测只省了 30 KB。

包大的真正原因是 **SQLCipher 的原生库占了四种 CPU 架构共 21 MB**。
真正把 41 MB 降到 9.5 MB 的是：

```kotlin
ndk { abiFilters += listOf("arm64-v8a") }
```

所以 debug 关掉 R8，release 保留。

代价说清楚：只打 arm64 的包**装不进 x86 模拟器**，也不支持 32 位老机器。
要发布给不确定的设备就用 `splits.abi` 拆分，而不是删掉。

### 10. commons 会覆盖你设的主题

`BaseSimpleActivity.onCreate()` 里会 `setTheme(getThemeId(...))`，
把你在 `super.onCreate()` **之前**设的主题替换掉。

表现：界面颜色完全不是你写的那套。解决办法是在 `super.onCreate()` **之后**
再设一次，或者干脆不继承它的 Activity（新工程就是这么做的）。

### 11. Room 遇到打不开的数据库会**直接删掉重建**

默认的 `DatabaseErrorHandler` 把「打不开」当成「损坏」，删文件、建空库，
**不抛异常、不留提示**。

这是那次数据丢失事故的起点：加密层没装上 → commons 建了个明文 Room 实例
去开 SQLCipher 加密的文件 → Room 认定损坏 → 删库重建 → 通讯录一瞬间变成空的。

防法（`EncryptedDatabases` 里已经做了）：

- `holdsOurInstance()` 做**同一性**判断，不能只判非空 ——
  字段里躺着别人建的明文实例时，判非空会返回 true
- 每次访问数据库前调 `requireReady()` 重新确认
- 装不上就**抛异常**，绝不返回空列表（空列表和「真的没有数据」长得一模一样）

---

## 四、一条铁律：不要从「扫描不到」推断删除

这是整个项目最重要的一条。

### 出过什么事

同步引擎原来的逻辑是「本地扫描时找不到的记录 = 用户删了 → 推墓碑」。

某次本地数据库有一瞬间读出空表（就是上面坑 #11 那条链），引擎于是把用户
全部 6 位联系人在服务器上一次性删光。而**墓碑会覆盖掉密文** ——
所有设备上同时永久消失。

最后是从 SQLite 的 WAL 里逐帧回滚才捞回来的。

### 为什么这个错误特别致命

两种错法的代价完全不对等：

- **漏报删除** → 服务器上多留一条已删的记录，用户再删一次就好
- **误报删除** → 全部设备上的数据被销毁，不可逆

### 现在的做法

1. **删除只在删除入口打标记**：`ContactRepository.deleteContact` /
   `PhoneData.deleteCall`。那是唯一权威的来源。
2. **扫描不到 = 异常，不是意图。** 对异常的正确反应是**用上次同步的快照
   把它恢复出来**（`SyncEngine.healVanished`），而不是扩大破坏。
3. **服务端有回收站**（`deleted_records` 表）。墓碑覆盖密文之前先归档一份。

> **还原时 rev 必须还原成原来的值。** 密文的 AAD 绑定了
> `uuid ‖ rev ‖ schemaVersion`，改了 rev 会让 AEAD 判定为伪造 ——
> 数据还在，但谁也打不开。相关接口：`POST /v1/sync/trash/restore`。

**新写任何同步逻辑时先问一句：这条路径有没有可能在数据没坏的情况下产生删除？**

---

## 五、两个 App 之间的边界

电话 App 拿到的**不是主密钥**，而是：

- `HKDF(DEK, "fc.collection.calls.v1")` 派生的子密钥
- 一个 **15 分钟**的短期访问令牌（刷新令牌不给）

这个区分是实打实的：电话 App 能加解密通话记录，但**推不回 DEK**，
所以它解不开任何一条联系人。万一电话 App 出漏洞被攻破，泄露的是通话记录，
不是整个通讯录。

配套的做法，改代码时请一起维护：

- 电话 App 的 `VaultCrypto` 是**裁剪版**。`createVault` / `wrapDek` /
  `deriveMasterKey` / 恢复码那一整套被**刻意删掉**了。
  不是因为编译不过，是因为**它不该有这些能力**。留着的话，哪天有人
  「顺手」在电话 App 里加个登录框，边界就没了，而那时候不会有任何编译错误提醒他。
- 依赖里也**没有 argon2**（那是从主口令派生密钥用的）。

**来电显示为什么要跨进程问通讯录**：算盲索引需要 DEK。把索引密钥交给电话 App，
它就能离线枚举任意号码是否在你通讯录里 —— 权限扩大了，收益只是省一次跨进程调用。

### 跨应用接口一览

| 通道 | authority | 给什么 |
| --- | --- | --- |
| 联系人 | `com.evelorion.contacts.privateprovider` | `/contacts` 全部联系人（含分组名）<br>`/lookup/<号码>` 按号码查人 |
| 保险库 | `com.evelorion.contacts.vaultbridge` | `/session` baseUrl + 短期令牌 + calls 子密钥 |

两边都有 `PrivacyGuard` 签名校验。不满足时返回**空 Cursor 而不是抛异常** ——
抛异常会让调用方闪退，反而等于告诉对方「这里确实有东西」。

---

## 六、还没做的

- **号码归属地**：没有离线号码库。在线查号会把「谁给你打电话」发给第三方，
  和端到端加密自相矛盾，所以宁可留空。
- **骚扰拦截**：同上。开关和界面在（`SpamShield`），判定逻辑等一份**本地**号码库。
- **通话录音**：界面有按钮，没接实现。
- **电话 App 的本地库是明文的**（`fc_calls.db`）。加密发生在**上传之前**。
  原因见 `CallDatabase` 里的注释：SQLCipher 的口令只在保险库解锁时拿得到，
  而来电时（锁屏、刚开机）往往拿不到，那会变成「有时候记不下通话记录」。
  它防的是服务器和网络，**不是** root 过的手机 —— 那个威胁模型在通讯录那边处理。
  **不要以为这里也是加密的。**
- 通讯录的 UI 是 XML 视图，电话的是 Compose。两套并存是刻意的：
  电话的设计稿本来就是 Compose，改写成 XML 会丢掉全部动画。

---

## 七、当前编译状态（交接时的实情）

写明白免得你踩空：

| | 状态 |
| --- | --- |
| `通讯录App/` | **编译通过**（含 provider 增加分组字段那次改动） |
| `电话App/` | 主体编译通过并出过包；**最后一批小改动没能再编一次** |
| `服务器后端/` | 编译通过，131 项 e2e 断言全过，已部署上线 |

「最后一批小改动」指这几处，都做过静态检查（注释/括号配平、资源引用、
函数签名与调用点比对），但**没有过编译器**：

- `CallDetailScreen` 填充通话历史（加了一个 `LaunchedEffect`）
- `CallActionSheet` 的「删除这条记录」接上 `PhoneData.deleteCall`
- `PhoneData` 增加 `deleteCall` / `historyFor` 改签名 / 改用 `Bg.single`
- `app/build.gradle.kts` 关掉 debug 的 R8

没能再编一次的原因：VPS 的 SSH 关掉了密码登录（这是好事，我建议过），
构建环境够不到了。**你在 Android Studio 里 Sync 一下就知道有没有问题**，
真有也应该是很浅的错误（缺 import、签名对不上这一类）。

---

## 八、安全待办

- [ ] **VPS root 密码要换。** 开发过程中被明文贴过，服务器上有 14000+ 次
      暴力破解尝试。已装 fail2ban（10 分钟错 5 次封一天），但密码本身该换。
- [ ] **管理后台密码要换。** 同样被明文贴过。
- [ ] **GitHub token 要删。** 同样。
- [ ] `PrivacyGuard.pinnedCertSha256` 还是空的，现在退回用 `checkSignatures`。
      发布前要填上真实签名证书的指纹。
- [ ] 考虑关掉 SSH 密码登录改用密钥
      —— **先配好密钥再关，否则会把自己锁在外面**。
- [ ] 用户的账号建于 `recovery_auth_hash` 功能上线之前，
      **需要改一次主口令**，否则恢复码登录不了服务器。
