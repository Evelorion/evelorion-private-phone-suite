<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 短信读写与收发 -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 读取通讯录 App 的私密联系人；系统只会把这两个 signature 权限
         授予使用同一发行证书签名的套件 App。 -->
    <uses-permission android:name="com.evelorion.permission.READ_PRIVATE_CONTACTS" />
    <uses-permission android:name="com.evelorion.permission.VAULT_BRIDGE" />

    <!-- 三个 App 都声明，消除安装顺序依赖，并让证书不一致在安装时直接失败。 -->
    <permission
        android:name="com.evelorion.permission.READ_PRIVATE_CONTACTS"
        android:description="@string/permission_read_private_contacts_desc"
        android:label="@string/permission_read_private_contacts_label"
        android:protectionLevel="signature" />

    <permission
        android:name="com.evelorion.permission.VAULT_BRIDGE"
        android:description="@string/permission_vault_bridge_desc"
        android:label="@string/permission_vault_bridge_label"
        android:protectionLevel="signature" />

    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <queries>
        <package android:name="com.evelorion.contacts" />
        <package android:name="com.evelorion.contacts.debug" />
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
    </queries>

    <application
        android:name=".SmsApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.SmsApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SmsApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 默认短信应用组件 3/4：ACTION_SENDTO -->
            <!-- 资格判定用：只有 SENDTO + DEFAULT + smsto，最贴合系统规范 -->
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="smsto" />
                <data android:scheme="sms" />
                <data android:scheme="mmsto" />
                <data android:scheme="mms" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SENDTO" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity>

        <!-- 默认短信应用组件 1/4：SMS_DELIVER（只有默认短信应用会收到） -->
        <receiver
            android:name=".sms.SmsDeliverReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <!-- 非默认短信应用时的兜底接收（SMS_RECEIVED） -->
        <receiver
            android:name=".sms.SmsReceivedReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- 默认短信应用组件 2/4：WAP_PUSH_DELIVER（彩信） -->
        <receiver
            android:name=".sms.MmsDeliverReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <!-- 实际投递彩信时用的 filter -->
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
            <!--
                默认短信应用「资格判定」用的 filter。
                系统用 mms: scheme + 该 mimeType 的 Intent 来匹配，
                而只声明 mimeType、不声明 scheme 的 filter 按规则只匹配
                content:/file: URI，匹配不上 -> 应用不会出现在短信应用候选里。
            -->
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:scheme="mms" android:mimeType="application/vnd.wap.mms-message" />
                <data android:scheme="mmsto" android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <!-- 发送状态 / 送达回执 -->
        <receiver android:name=".sms.SmsStatusReceiver" android:exported="false" />

        <!-- 通知里的「标记已读 / 快捷回复」 -->
        <receiver android:name=".sms.NotificationActionReceiver" android:exported="false" />

        <!-- 默认短信应用组件 4/4：Headless 快捷回复 Service -->
        <service
            android:name=".sms.HeadlessSmsSendService"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

    </application>
</manifest>
