<?xml version="1.0" encoding="utf-8"?>
<!--
  筛选 chip。选中 = 填充 secondary_container 且**没有描边**；
  未选中 = 透明 + 1dp 描边。

  M3 要求两者只能有一个有描边 —— 都有的话选中态看起来像「被框住」
  而不是「被选中」。
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android" android:color="?attr/colorOnSurfaceVariant">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/chip_radius" />
            <solid android:color="#FFFFFF" />
        </shape>
    </item>
    <item>
        <selector>
            <item android:state_selected="true">
                <shape android:shape="rectangle">
                    <corners android:radius="@dimen/chip_radius" />
                    <solid android:color="?attr/colorSecondaryContainer" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="@dimen/chip_radius" />
                    <solid android:color="@android:color/transparent" />
                    <stroke android:width="1dp" android:color="?attr/colorOutlineVariant" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
