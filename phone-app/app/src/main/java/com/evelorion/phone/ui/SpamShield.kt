package com.evelorion.phone.ui

/**
 * 骚扰拦截的开关。
 *
 * 目前只是个开关位：真正的号码标记库需要一份离线数据（或者一个在线接口），
 * 而在线查号会把「谁给你打电话」这件事发给第三方 —— 对一个主打
 * 端到端加密的 App 来说，那是自相矛盾的。
 *
 * 所以先把开关和界面留在这里，等接一份**本地**号码库再让它真正生效，
 * 而不是先接一个会外泄的在线服务。
 */
object SpamShield {
    var enabled: Boolean = true
}
